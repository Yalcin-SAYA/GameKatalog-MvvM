<Window x:Class="GameKatalog_MvvM.Views.RPGSpielView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameKatalog_MvvM.Views"
        mc:Ignorable="d"
        Title="RPGSpielView" MinWidth="800" MinHeight="450" WindowStartupLocation="CenterScreen" KeyDown="Window_KeyDown">

    <DockPanel>

        <!-- Statusleiste -->

        <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Top"
                    Background="LightGray"
                    Height="30">

            <TextBlock x:Name="HpText"
                       Margin="10,5"
                       FontWeight="Bold"
                       Text="{Binding HpText}" />

            <TextBlock x:Name="GoldText"
                       Margin="20,5"
                       FontWeight="Bold"
                       Text="{Binding GoldText}" />

        </StackPanel>

        <!-- Spielfeld -->

        <Grid>

            <UniformGrid x:Name="MapGrid"
                         Rows="20"
                         Columns="20"/>

        </Grid>

    </DockPanel>

</Window>